"use client"
import React from 'react';
import { Spinner as MTSpinner } from '@material-tailwind/react';

const Spinner = () => {
    return <MTSpinner color="indigo" className="h-16 w-16 text-gray-color/50" />;
}

export default Spinner;